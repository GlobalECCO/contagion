apply plugin: 'base'

task dist(type:Zip) {
  baseName = "app"
  destinationDir = new File("$projectDir/target")
  includes = [
    "client/",
    "lib/",
    "node_modules/",
    "server/",
    "shared/",
    "Contagion-game-icon.png",
    "app.js",
    "gamepage.html",
    "index.html",
    "package.json"
  ]


  from projectDir

}